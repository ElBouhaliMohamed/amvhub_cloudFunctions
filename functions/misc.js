const functions = require('firebase-functions');

exports.appCheck = function (context) {
  if (context.app == undefined) {
    throw new functions.https.HttpsError(
      'failed-precondition',
      'The function must be called from an App Check verified app.'
    );
  }
};
